# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# OpenAI — exclusivo para embeddings (text-embedding-3-small)
OPENAI_API_KEY=sk-...

# GLM API key — chave do Zhipu AI para o modelo extrator (LLM_MODEL)
# Se definido, substitui OPENAI_API_KEY nas chamadas de LLM (não afeta embeddings)
GLM_API_KEY=

# Application
APP_ENV=development           # development | production
APP_PORT=8000
APP_HOST=0.0.0.0

# Worker polling interval in seconds
WORKER_POLL_INTERVAL=5

# Analysis settings
EMBEDDING_MODEL=text-embedding-3-small
LLM_MODEL=gpt-4o-mini          # Bulk extractor — swap to glm-4-flash for free tier
LLM_MODEL_HEAVY=gpt-4o
LLM_MODEL_CONSOLIDATOR=anthropic/claude-sonnet-4-6  # Final reviewer (KnowledgeConsolidator Phase 2)

# Anthropic — used by KnowledgeConsolidator as final reviewer
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI-compatible base URL — set for GLM-4 (Zhipu) or other providers
# Zhipu GLM-4: https://open.bigmodel.cn/api/paas/v4/
# If set, LLM_MODEL will be called with this base URL (e.g. glm-4-flash)
OPENAI_BASE_URL=

# Report output directory (local only, ignored in production)
REPORTS_OUTPUT_DIR=./legacy-analyzer
